spring:
  application:
    name: spikes-graph-api-client

server:
    port: 8080

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true

#eureka:
#  client:
#  fetch-egistry: true
#  registerWithEureka: true
#  service-url.defaultZone: http://localhost:8761/eureka
